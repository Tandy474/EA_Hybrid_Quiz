class User:
    def __init__(self, username, password):
        self.username = username    # stores the username for login
        self.password = password    # stores the user's password (plain text for demo)
